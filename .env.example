# GitHub Personal Access Token
# Replace 'your_github_token_here' with your actual GitHub PAT
GITHUB_PAT=your_github_token_here

# Example usage:
# - For GitHub CLI: gh auth login --with-token < .env (after setting the token)
# - For applications: process.env.GITHUB_TOKEN
# - For scripts: export $(cat .env | xargs) && echo $GITHUB_TOKEN

# Important: 
# 1. Never commit actual tokens to version control
# 2. Keep this file in .gitignore
# 3. Generate tokens at: https://github.com/settings/tokens
# 4. Use minimal required scopes for security
